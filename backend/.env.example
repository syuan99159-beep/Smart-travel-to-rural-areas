# Google Maps API key (do NOT commit the real key)
GOOGLE_MAPS_API_KEY=
# Set to true to enable real Google Maps queries (default false)
USE_REAL_MAPS=false

# Other environment variables may be added here
